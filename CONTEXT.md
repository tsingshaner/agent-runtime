# Agent Runtime

统一管理不同 agent runtime 的会话、执行及项目资源。

## Language

**Project（项目）**：会话、记忆和资源绑定的归属单位，以稳定的项目标识识别。一个项目可以包含多个工作目录，目录移动不改变项目身份；项目归属本身不构成文件访问沙箱。

**Resource Binding（资源绑定）**：项目对资源的显式选择及启停设置。项目可用的技能与 MCP 由启用清单确定，不自动追加用户全局配置中的资源。

**Session（会话）**：属于一个项目、固定绑定一种 runtime 的持续交互上下文。会话不跨 runtime 迁移。

**Run（运行）**：会话中的一次执行尝试，等待审批或补充输入时仍保持活动。同一会话最多有一个活动运行，不同会话可以并行运行。

**Task（任务）**：通过 A2A 表达的一项工作，首版与一次运行一一对应，但使用独立标识。等待审批或补充输入时保留原任务，后续新的工作创建新任务。
_Avoid_: 将 Task 与 Run 互作别名

**Approval（审批）**：对一项待执行操作作出批准或拒绝的请求。等待审批仍属于当前运行。

**Approval Batch（审批批次）**：必须收齐每项决定后才能一起交回 runtime 的一组审批。组内每项操作可分别批准或拒绝。

**Input Request（输入请求）**：执行过程中对补充信息的请求，其回答可以是文字或问题答案。
_Avoid_: 将补充信息统称为审批

**Request ID（请求标识）**：调用方为一次运行提交提供的可选标识。同一会话内，重复提交该标识及相同输入指向同一次运行，而不是新的执行尝试。

**Knowledge（知识）**：项目绑定、以 Markdown 文档维护并按需检索和读取的显式知识。
_Avoid_: Wiki、wiki 接口别名

**Memory（记忆）**：从会话内容中积累、可在后续执行时召回的信息。同一项目的不同 runtime 共用记忆，不同项目的记忆相互隔离。

**Memory Write（记忆写入）**：将一次成功运行的新增对话交给记忆服务的独立处理记录。聊天成功不表示该记录已被记忆服务接收，其状态可独立查询。

**Skill（技能）**：以 SKILL.md 描述、可附带脚本和参考资料的能力单元。项目绑定决定该技能是否可供其 runtime 使用。
