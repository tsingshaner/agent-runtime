# 仅将成功运行的新增对话写入记忆

长期记忆只接收成功运行新增的用户输入与最终助手回复，并以项目、会话和运行标识来源。相比每轮上传完整会话或全部执行轨迹，这会舍弃部分工具细节，但避免重复写入历史内容、回灌召回内容以及将注入文档或失败运行的部分结果当作新的记忆来源。

运行成功状态、唯一成功终态事件及待写记忆记录在本地同一事务提交，随后立即发布成功终态；后台再写 MemoryCore。记忆写入状态与降级原因通过独立查询提供，不向已结束的 Run 追加第二个终态，也不因记忆服务写入失败改判聊天失败；结果不确定的写入保留记录，不自动补写。
